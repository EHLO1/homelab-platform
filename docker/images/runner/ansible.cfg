[defaults]
inventory = inventory/homelab.proxmox.yml
private_key_file = ~/.ssh/id_ansible
roles_path = roles
stdout_callback = yaml
nocows = True
retry_files_enabled = False
interpreter_python = auto_silent

[ssh_connection]
# Fewer SSH round-trips per task. Works on modern distros (no requiretty).
pipelining = True